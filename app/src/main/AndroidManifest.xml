<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.icon.agnks" >

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".BaseActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DevicesManageActivity"
            android:label="@string/action_manage_devices" >
        </activity>
        <activity
            android:name=".ProgrDeviceActivity"
            android:label="@string/action_progr_device" >
        </activity>
        <activity
            android:name=".EditDeviceActivity"
            android:label="@string/action_edit_device" >
        </activity>
        <activity
            android:name="com.icon.bluetooth.TestActivity"
            android:label="@string/action_test" >
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/action_settings" >
            <intent-filter>
                <category android:name="android.intent.category.PREFERENCE" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AboutActivity"
            android:label="@string/action_about" >
        </activity>
        <activity
            android:name=".LogActivity"
            android:label="@string/action_logs" >
            <intent-filter>
                <action android:name="com.icon.agnks.LogActivity" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>
